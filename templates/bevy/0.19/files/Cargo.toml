[package]
name = "{{crate_name}}"
version = "0.1.0"
edition = "2021"

[dependencies]
bevy = "=0.19.1"

# Bevy's recommended dev profile: fast dependency builds, debuggable app code.
[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3
